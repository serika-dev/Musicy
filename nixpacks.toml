[phases.setup]
# ffmpeg is required for generating streaming-quality renditions (transcode.ts)
nixPkgs = ["...", "bun", "ffmpeg"]

[phases.install]
cmds = ["bun install"]

[phases.build]
cmds = ["bun run build"]

[start]
cmd = "bun run start"
