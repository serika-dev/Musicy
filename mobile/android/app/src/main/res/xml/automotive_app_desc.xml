<?xml version="1.0" encoding="utf-8"?>
<!-- Only documented `uses` values belong here. An unrecognised entry makes the
     Android Auto host reject the whole descriptor, which silently removes the
     app from the car's launcher. -->
<automotiveApp>
    <uses name="media" />
</automotiveApp>
